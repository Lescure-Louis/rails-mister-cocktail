 <%= simple_form_for [@cocktail] do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>

  <%= f.button :submit, class: "btn btn-outline-primary" %>

  <% end %>
<p class="btn btn-outline-primary" ><%= link_to "Back to home", cocktails_path%></p>
